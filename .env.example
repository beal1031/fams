# Facebook Graph API credentials (local development only)
# Copy this to .env and fill in your values to test the fetch script locally
# WARNING: Never commit .env to the repo — add it to .gitignore

FB_TOKEN=your_page_access_token_here
FB_PAGE=fraenamammenschetter
